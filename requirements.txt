# Default runtime and evaluation dependencies.
#
# This manifest intentionally excludes CUDA-built SSM kernels. Install
# requirements-ssm-cuda.txt only on CUDA-devel hosts that can compile or fetch
# compatible mamba-ssm / causal-conv1d wheels.

# Core PyTorch and ML libraries
torch>=2.1.0
transformers>=4.55.0,<4.56
accelerate>=1.6.0
safetensors>=0.5.3

# Tensor operations and utilities
einops>=0.7.0
numpy>=1.26.0

# Configuration and data handling
PyYAML>=6.0.0
datasets>=3.6.0
huggingface-hub>=0.34.0

# Logging and experiment tracking
rich>=13.7.0
loguru>=0.7.0
wandb>=0.16.0

# Evaluation
lm_eval==0.4.11
pandas>=2.2.0

# Learning rate scheduler
pytorch-lightning>=1.9.5  # Required for pl_bolts.optimizers.lr_scheduler

# Data generation utilities (for NeedleInHaystackDataset)
nltk>=3.9.0
wonderwords>=2.2.0

# Visualization (for tools)
matplotlib>=3.8.0

# Testing (optional but recommended)
pytest>=8.3.0
