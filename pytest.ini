[pytest]
markers =
    cuda: requires NVIDIA CUDA hardware
    multi_gpu: requires two or more visible CUDA GPUs
    hf: requires Hugging Face transformers/model cache
    network: requires network access or remote model/dataset downloads
    slow: slower smoke tests that may load models or spawn subprocesses
    optional_deps: requires optional research dependencies
